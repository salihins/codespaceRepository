{
  "name": "interview-workspace",
  "version": "1.0.0",
  "scripts": {
    "test": "vitest run",
    "test:debug": "vitest --inspect-brk --no-file-parallelism --threads=false",
    "test:js": "vitest run js/",
    "test:ts": "vitest run ts/"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
